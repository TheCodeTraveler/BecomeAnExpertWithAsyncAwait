@inherits CodeLinkBase

<a class="code-link" href="@Location.GetUrl(Editor)" target="@Target" rel="@Rel" title="@Title"><code>@(Label ?? Location.FileName)@(Location.Line is { } line ? $":{line}" : string.Empty)</code></a>
